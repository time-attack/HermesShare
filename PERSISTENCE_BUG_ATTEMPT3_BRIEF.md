HermesShare — persistence bug, 3rd attempt, THIS TIME SHOW REAL EVIDENCE IN YOUR REPORT

## Non-negotiable requirement for your final report
Two previous sessions on this exact bug both claimed success with plausible-sounding root-cause narratives, shipped IPAs, and the bug was STILL LIVE both times when the user actually tested. Do not repeat this pattern. Your final report MUST include, verbatim, pasted:
- Actual on-device debug log excerpts (real timestamps, real session keys, real code-path names) showing the bug happening BEFORE your fix, if you can get device access at any point (check `xcrun devicectl list devices` repeatedly through your session - device availability has been flaky, grab logs the moment it connects).
- If the device is never reachable this session, then instead show actual PASSING test output (not a claim that tests pass - the literal test runner output text) for a NEW test that reproduces the SPECIFIC new failure mode described below, and be explicit that on-device confirmation is still outstanding.
- Do not write a narrative-only report with no pasted raw evidence. If you cannot produce real logs or real test output, say so explicitly rather than asserting confidence you don't have.

## Two new, sharper clues from the user (this session, verbatim)
1. "I guess the issue is sending multiple at a time" - the user reports the bug reproduces when MULTIPLE HermesShare cards are sent into the conversation in relatively quick succession (via the Photon send script, multiple calls close together), but did NOT reproduce when a single card was sent and tested alone. This strongly suggests a RACE CONDITION - something shared/global being written or read while a previous send/receive/cache-store is still in flight, not a simple one-shot logic bug in a single presentContent call.
2. "could it be there is a bug and it's falling back to the last one" - after sending a batch of cards (a 15-option "Scholarship Matches" picker was the most recent card sent before a "Flight BR 26" card), the user reports getting stuck showing the SCHOLARSHIP card repeatedly no matter what they tap - i.e., stuck on the MOST RECENTLY SENT/CACHED card specifically, not the first card ever tapped. This is a meaningfully different signature than the original bug report (which was "stuck on the FIRST card tapped") - re-examine whether the current code has ANY remaining path that resolves to "most recent" rather than "the specific message that was actually tapped or selected."

## What to actually do
1. Read the CURRENT, ACTUAL state of MessagesViewController.swift and HermesLayoutSessionCache (or wherever it now lives - it may have been renamed/moved by a previous session, verify current file locations, don't assume from memory of prior reports) fresh - do not trust any previous session's description of what it left behind.
2. Specifically hunt for: (a) any global mutable state (a singleton, a static var, a shared cache with a "most recent" or "latest" accessor) that could be written by one message's didReceive/didSelect/insert flow while another message's presentContent is concurrently reading it - this is the concrete race-condition hypothesis raised by clue #1; (b) any code path - even one you think you already closed off in a previous session - that can still resolve to "the last cached/received layout" rather than a layout keyed specifically to the tapped/selected message's own session - this is clue #2's hypothesis, and given the user's new report (\"stuck on the LAST one now, not the FIRST one\"), specifically check whether a previous session's own fix accidentally introduced or left in a subtly different last-resort branch.
3. If you find a global/shared mutable cache being touched from multiple call sites without any serialization (e.g. didReceive on one message writing a "latest" pointer while a different message's tap is resolving), that IS the race condition - fix it properly: either make each message's cache entry fully independent with no shared "latest" pointer at all (preferred - a "latest" accessor is inherently the wrong abstraction for a multi-card conversation, remove it entirely rather than patch around it), or add proper synchronization if a shared pointer is genuinely still needed for some other legitimate purpose (check if anything else in the codebase actually depends on a "latest" accessor before deciding it's needed).
4. Build a NEW test that specifically simulates the two new clues: (a) insert/receive multiple DIFFERENT cards in quick succession (simulate the store/cache writes happening close together, not sequentially-safe far apart), then (b) tap-open a card that is NOT the most recently inserted one, and assert it renders correctly - not the most recent one. This is a meaningfully different test scenario than what either previous session built (both tested "tap A then tap B" sequentially with time to settle between - neither tested "several cards land close together, then an older one is tapped").
5. Add debug logging (if not already sufficient) that would make a REAL captured log unambiguous about which specific code path/cache entry key was consulted at each step, so if the user reports this bug again, whoever looks at it next has an actual paper trail rather than needing another guessing round.

## Constraints - do not break these
- Keep .safeAreaInset(edge: .bottom) pinned-action-bar pattern intact.
- Keep the ADA-doctrine visual redesign (flightBoard, platedDish, gaugeCluster, cabin-framed seatChart, enriched timeline, optionPicker with badge/sublabel) intact - this bug is purely navigation/persistence/caching, not visuals.
- Photon transport: customizedMiniApp() needs https:// URLs with a p= query param; layout.image needs JPEG bytes, not PNG.
- Fixed JSON-schema interpreter model must not be broken - no arbitrary code execution.

## Delivery (same pipeline as before, verify before reporting done)
1. Build Release for generic iOS device (xcodebuild -destination "generic/platform=iOS").
2. Package + host a fresh .ipa exactly per ~/.hermes/skills/software-development/hermesshare-ipa-sideload/SKILL.md. Curl-verify HTTP 200 AND exact byte-size match against the file on disk BEFORE writing the link into your report. Two previous sessions' links died before the user could use them (their background tunnel/http.server processes die when the session exits) - explicitly note in your report that this link needs re-verification if significant time passes before the user clicks it, and that this is a known, real limitation of ephemeral quick-tunnels, not something to paper over.
3. Send 4-5 fresh test cards through Photon IN QUICK SUCCESSION (deliberately reproducing clue #1's condition - back-to-back send calls, not spaced out) into the user's conversation (PHOTON_ALLOWED_USERS from ~/.hermes/.env; use ~/.hermes/hermes-agent/plugins/platforms/photon/sidecar/send_card_photon_v3.mjs), each with a real restrained JPEG thumbnail (reuse/extend ~/.hermes/skills/productivity/hermesshare-cards/scripts/make_thumbnails_restrained.py). Include at least one optionPicker with many options (mirroring the "Scholarship Matches" 15-option card that triggered this) and at least 2 other distinct card types, so the user can tap around between all of them, including tapping an OLDER card after several newer ones have landed - this is the specific scenario that needs to work now.
4. Your final report must include the actual raw evidence (log excerpt or literal test output) per the non-negotiable requirement above, not just a narrative claim of success.
